# @package _global_
#
# MViT V2-S model configuration.
# Selected via: torchrun ... src/runs/train.py model=mvit

model_type: mvit_finder
classifier_model: shallow_mv_aggregate

embedding_dim: 768
num_frames: 100

frame_selection:
  enable: false
  num_queries: 2
  num_frames: 16
  num_probes: 8
